// ignore_for_file: prefer_const_constructors

import 'package:flutter_test/flutter_test.dart';

import '../../lib/splash.dart';

void main() {
  group('Splash', () {
    test('can be instantiated', () {
      expect(Splash(), isNotNull);
    });
  });
}
